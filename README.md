# Blender-Texture-Cycle
A script which loops through all actively used textures in a Blender project and changes the shader of each one. 

Primary features:
- Stores the diffuse of the initial shader setup
- Memory of the previous node to be connected to the output
- Ignore inactive textures
